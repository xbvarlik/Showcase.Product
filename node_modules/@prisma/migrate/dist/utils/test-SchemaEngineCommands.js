"use strict";
var import_internals = require("@prisma/internals");
async function main() {
  const result = await (0, import_internals.canConnectToDatabase)(process.env.TEST_CONNECTION_STRING);
  console.log(result);
}
void main();
