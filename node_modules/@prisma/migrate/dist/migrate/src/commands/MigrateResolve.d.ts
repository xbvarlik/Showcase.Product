import { Command, HelpError } from '@prisma/internals';
export declare class MigrateResolve implements Command {
    static new(): MigrateResolve;
    private static help;
    parse(argv: string[]): Promise<string | Error>;
    help(error?: string): string | HelpError;
}
