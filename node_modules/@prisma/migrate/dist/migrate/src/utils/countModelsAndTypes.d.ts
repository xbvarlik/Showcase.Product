import { MigrateTypes } from '@prisma/internals';
type Counts = {
    modelsCount: number;
    typesCount: number;
};
export declare function countModelsAndTypes(schemas: MigrateTypes.SchemasContainer): Counts;
export {};
