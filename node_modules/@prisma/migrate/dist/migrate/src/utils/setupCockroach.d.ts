export declare function setupCockroach(options: {
    connectionString: string;
    dirname: string;
}): Promise<void>;
export declare function tearDownCockroach(options: {
    connectionString: string;
}): Promise<void>;
